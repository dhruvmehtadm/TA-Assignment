def pattern(p):
   X = 2*p - 2
   for i in range(0, p):
      for j in range(0, X):
         print(end=" ")
      X = X - 2
      for j in range(0, i+1):
         print("* ", end="")
      print("\r")
p = 5
pattern(p)
